# Validation workflow

This folder is intended for external validation scripts and outputs. It is excluded from R package builds through `.Rbuildignore` so that large downloaded DWD data and result files are not bundled into CRAN-style source packages.
