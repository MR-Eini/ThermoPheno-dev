# Default crop parameters

Default crop parameters. See the README and function source
documentation for detailed arguments.

## Usage

``` r
default_parameters(...)
```

## Value

A model result, helper object, or side effect depending on the function.
